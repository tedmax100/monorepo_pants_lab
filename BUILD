# Root BUILD file
# Go module target — teaches Pants about our Go module and its third-party deps
go_mod(name="mod")

# Python third-party dependencies from requirements.txt
python_requirements(name="reqs")
